/* const myList = document.querySelector('.list');
const oneItem = myList.querySelector('.item-1');
const twoItem = oneItem.nextElementSibling;
const elem = twoItem.getBoundingClientRect();
console.log(elem);
console.log(elem.height) */

/* const myPromise = new Promise(
    (resolve) => {
        setTimeout (
            () => {
                console.log('Привет, я функция-исполнитель!')
            }, 2000
        )
    }
);
 */

console.log(this);